---
interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-8">
    <h2 class="text-3xl font-bold text-white">{title}</h2>
    <p class="mt-2 text-[#828282] text-xl">{subtitle}</p>
</div>